{
  "queries": {
    "TypKosztu": { "sql": "SELECT id, nazwa as text FROM items", "sqlCount": "SELECT COUNT(*) AS total FROM items" },
    "UsersActive": { "sql": "SELECT id, nazwa as text FROM items WHERE czyAktywny = ? AND nazwa = ?", "sqlCount": "SELECT COUNT(*) AS total FROM items WHERE czyAktywny = ? AND nazwa = ?" },
    "UserByName": { "sql": "SELECT * FROM users WHERE username = ? AND czyAktywny = ?", "tableName": "users" }
  }
}